import { memo } from "react";
import { View } from "react-native";

interface MentalProps {}

export const MentalPage = memo((props: MentalProps) => {
  const {} = props;

  return <View></View>;
});
