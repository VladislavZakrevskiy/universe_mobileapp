import { memo } from "react";
import { View } from "react-native";

interface PoliticsProps {}

export const PoliticsPage = memo((props: PoliticsProps) => {
  const {} = props;

  return <View></View>;
});
