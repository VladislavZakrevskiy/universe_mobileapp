import { HStack, ScrollView, VStack } from "native-base";
import { SchoolTable } from "../../../../entities/School";

const InfoLesson = () => {
  return <HStack>

  </HStack>
}

export function DailyPage() {
  return (
    <ScrollView>
      <SchoolTable />
    </ScrollView>
  );
}
