import { memo } from 'react';
import { View } from 'react-native'

interface PoliticsProps {

}

export const Politics = memo((props: PoliticsProps) => {
    const {  } = props;
    
    return (
        <View>
            
        </View>
    );
});