import { memo } from 'react';
import { View } from 'react-native'

interface MentalProps {

}

export const Mental = memo((props: MentalProps) => {
    const {  } = props;
    
    return (
        <View>
            
        </View>
    );
});